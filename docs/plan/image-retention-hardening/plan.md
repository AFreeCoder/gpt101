# 生产镜像保留策略执行计划

日期：2026-07-15

关联设计：`docs/design/image-retention-hardening/design.md`

## 执行任务

- [x] 为镜像保留脚本补充行为测试，先验证当前实现不满足需求。
- [x] 实现支持预览的镜像保留脚本，覆盖版本去重、三版本保留、临时镜像清理和容器引用保护。
- [x] 将清理脚本接入生产发布工作流，并确保调用位于全部健康检查之后。
- [x] 修正按提交号从 GHCR 回滚时的镜像标签格式。
- [x] 更新常青部署文档中的本地保留策略、GHCR 回滚路径和运维命令。
- [x] 运行部署专项测试、Shell 语法检查、格式检查、完整测试、Lint 和构建。
- [x] 检查最终差异和发布前状态；未经确认不推送、不触发生产部署。

## 验收检查

- [x] 成功发布路径只在全部健康检查通过后调用清理。
- [x] 当前镜像和最近两个不同的成功生产镜像得到保留。
- [x] 更旧的成功镜像和未成功上线的本地 `sha-*` 镜像得到清理。
- [x] 仍被容器引用的镜像不会被强制删除。
- [x] 非 GPT101 镜像和 GHCR 远端版本不受影响。
- [x] 提交号回滚使用 `sha-<完整提交号>`。
- [x] 验证结果已记录并形成可发布结论；仓库既有格式基线问题单独列明。
