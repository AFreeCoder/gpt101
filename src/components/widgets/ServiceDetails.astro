---
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';

interface Props {
  id?: string;
  isDark?: boolean;
  classes?: Record<string, string>;
  bg?: string;
}

const { id, isDark = false, classes = {}, bg = await Astro.slots.render('bg') } = Astro.props;
---

<WidgetWrapper id={id} isDark={isDark} containerClass={`max-w-6xl ${classes?.container ?? ''}`} bg={bg}>
  <div class="text-center mb-16">
    <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4">ChatGPT Plus 充值步骤</h2>
  </div>

  <div class="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto mb-12">
    <!-- Step 1 -->
    <div class="text-center">
      <div
        class="bg-white rounded-xl p-6 shadow-md border border-gray-100 hover:shadow-lg transition-shadow duration-300"
      >
        <div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <span class="text-2xl font-bold text-blue-600">1</span>
        </div>

        <h3 class="text-lg font-bold text-gray-800 mb-3">🛒 购买卡密</h3>
        <p class="text-sm text-gray-600 mb-4">选择Plus套餐，微信支付完成订单，立即获得卡密</p>

        <div class="bg-blue-50 rounded-lg p-3">
          <div class="flex items-center justify-center gap-2 text-blue-700 text-sm font-medium">
            <span>💳</span>
            <span>支持微信支付</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 2 -->
    <div class="text-center">
      <div
        class="bg-white rounded-xl p-6 shadow-md border border-gray-100 hover:shadow-lg transition-shadow duration-300"
      >
        <div class="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <span class="text-2xl font-bold text-purple-600">2</span>
        </div>

        <h3 class="text-lg font-bold text-gray-800 mb-3">🔐 验证卡密</h3>
        <p class="text-sm text-gray-600 mb-4">根据提示，输入卡密，验证卡密有效性</p>

        <div class="bg-purple-50 rounded-lg p-3">
          <div class="flex items-center justify-center gap-2 text-purple-700 text-sm font-medium">
            <span>🔐</span>
            <span>安全验证</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Step 3 -->
    <div class="text-center">
      <div
        class="bg-white rounded-xl p-6 shadow-md border border-gray-100 hover:shadow-lg transition-shadow duration-300"
      >
        <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <span class="text-2xl font-bold text-green-600">3</span>
        </div>

        <h3 class="text-lg font-bold text-gray-800 mb-3">✅ 完成充值</h3>
        <p class="text-sm text-gray-600 mb-4">根据提示核对账户等信息，系统自动升级账户</p>

        <div class="bg-green-50 rounded-lg p-3">
          <div class="flex items-center justify-center gap-2 text-green-700 text-sm font-medium">
            <span>✅</span>
            <span>即时生效</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- 安全保障 -->
  <div class="bg-gradient-to-r from-blue-50 to-purple-50 rounded-xl p-8 mb-8">
    <div class="text-center mb-6">
      <h3 class="text-xl font-bold text-gray-800 mb-4">🛡️ 安全保障</h3>
    </div>

    <div class="grid md:grid-cols-4 gap-6 text-center">
      <div>
        <div class="text-lg font-bold text-blue-600 mb-1">iOS正规技术</div>
        <p class="text-sm text-gray-600">正规Apple账户充值通道</p>
      </div>
      <div>
        <div class="text-lg font-bold text-green-600 mb-1">官方直充</div>
        <p class="text-sm text-gray-600">直接向ChatGPT官方充值</p>
      </div>
      <div>
        <div class="text-lg font-bold text-purple-600 mb-1">99.5%成功率</div>
        <p class="text-sm text-gray-600">成熟技术和流程</p>
      </div>
      <div>
        <div class="text-lg font-bold text-orange-600 mb-1">失败退款</div>
        <p class="text-sm text-gray-600">充值失败100%退款</p>
      </div>
    </div>
  </div>
</WidgetWrapper>
