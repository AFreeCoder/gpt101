# 2025-11-12 SEO 记录

## 思路（已确认）
- **整体SEO策略**：围绕「ChatGPT Plus代充」「ChatGPT镜像」「国内支付」「OpenAI充值」等高意图关键词，构建覆盖认知-比较-决策-售后全漏斗的内容簇；在 Astro 现有框架内，通过模块化 meta/OG/schema、补齐站点导航与落地页，提升可索引内容与点击率。
- **内容增长方向**：交易/转化型教程（充值流程、支付方式、常见故障）、比较评测（镜像 vs 官网、镜像横评、Pro价值分析）、教程学习型（AI学习路径、提示词合集、行业案例），并将内容与 CTA 深度耦合。
- **外链与品牌声量**：利用自有矩阵（B站/小红书/知乎/公众号）发布教程，争取 AI 工具导航、开发者社区等客座文章或互链，同时引导真实用户评价形成「品牌词+代充」组合曝光。
- **数据反馈闭环**：依托已接入的 GA4 / 百度统计配置事件追踪（按钮点击、外链跳转、表单等），用数据反推内容选题与转化优化。

## 方案（需后续细化）
1. **信息架构**：启用 `/tutorials` 或新建“使用教程/充值指南”导航分支，规划 3–5 个核心落地页（Plus代充教程、镜像体验、国内支付指南等），并在首页/FAQ/Footer 设置互链形成清晰站内链接网。
2. **内容生产**：基于 `PageLayout` 与 Markdown/MDX 建立统一模板（痛点→方案→步骤→FAQ→CTA），优先撰写「ChatGPT Plus 代充流程」和「ChatGPT 镜像使用体验」两篇 1200+ 字文章，包含截图/步骤列表及站内内链。
3. **SEO 技术要素**：为新页面配置独立 `metadata`、JSON-LD FAQ/HowTo schema，更新 sitemap 与 robots，确保每个模块均可被索引；拆分 Plus 与镜像 CTA，减少重复外链。
4. **转化链路**：在教程页嵌入弹窗/按钮指向站内购买入口，配套优惠、退款、客服、信任背书模块，并用 GA4 事件追踪内容到转化路径。
5. **外部发布**：将站内长文裁剪成多平台图文/短视频，发布到知乎/小红书/公众号等，统一回链到对应落地页，获取初始外链与品牌曝光。

## 待确认
- `/tutorials` 作为内容聚合页的具体信息架构（分类、分页、URL 规范）及是否需要国际化版本。
- 首批 3–5 篇落地页的最终主题、字数、是否需要多媒体（视频/截图）。
- JSON-LD 方案是否全站统一封装为组件，便于复用。
- 外部矩阵渠道的优先顺序与可投入资源（人手/预算）。

## 优先方向记录
- **优先方向**：建立高意图落地页并解决内容缺失——通过新增教程/体验页快速增加可索引文本和长尾流量，同时强化 CTA 转化。
- **行动要点**：在站内完成导航落位、内容模板、首批文章，并将成品同步到外部渠道获取回链。

---

## 追加记录（2025-11-12 进一步讨论）

- **现有内容库位置**：历史文章位于 `src/data/post`，目前包含 `how-to-upgrade-gpt-plus.mdx`、`chatgpt-mirror-guide.mdx`、`2025-latest-7-way-to-upgrade-chatgpt-plus.mdx` 等，可作为统一模板改造的起点。
- **“统一模板”定义**：
  - **文章结构层**：规范 frontmatter 字段（title、excerpt、publishDate、tags、metadata 等）、正文章节顺序（痛点/概述 → 步骤/体验 → FAQ/售后 → CTA），确保所有 MDX 文档遵循相同骨架，便于批量生产与 SEO 优化。
  - **渲染样式层**：在 Astro 侧提供统一的 Markdown/MDX Layout（例如 `ContentLayout.astro`），集中处理排版、目录、CTA 区块、JSON-LD 注入等渲染逻辑，保证不同文章在 UI/交互上一致且可复用新组件（如截图说明、步骤卡片）。
- **当前任务**：从“信息架构 + 内容模板”入手，先盘点并重构现有 MDX（尤其是 `how-to-upgrade-gpt-plus.mdx`），在导航中新增教程入口，再迭代到新增文章。

## 前端落地（Frontmatter & Layout）执行说明

- **Frontmatter 规范**（本阶段改动范围）：
  - 固定字段：`title`、`publishDate`、`updateDate`（可选）、`excerpt`、`tags`、`image`（可选）、`category`（可选）、`metadata`（含 canonical/description/OG）。
  - 约定：`excerpt` 控制在 130~160 字内，兼做 meta description；`metadata.description` 默认复用 `excerpt`，除非另有需求。
  - 若文章需要自定义 canonical/OG，则在 `metadata` 中声明；未提供时由 `getStaticPaths` 逻辑补足。
  - 现有三篇 MDX 将率先对齐字段，并保持正文内容不做改动。
- **Layout 改造**：
  - 以 `src/components/blog/SinglePost.astro` 为基础，引入统一的文章头部（标题/发布日期/更新日期/阅读时长）与右侧 CTA 辅助区，确保所有文章视觉一致且自动带出站内行动按钮。
  - 文章主体依旧渲染来自 MDX 的 `<slot />`，不干预正文；CTA/客服信息等统一通过组件注入，便于后续集中维护。

## 信息架构 & 导航优化（进行中）

- **现状评估（/tutorials）**：
  - 列表页位于 `src/pages/[...blog]/[...page].astro`，沿用 Astrowind 默认 Blog 模板，缺少教程定位（标题仍为英文“Blog”、Hero/分类为空）。
  - 单篇“返回”入口 `src/components/blog/ToBlogLink.astro` 仍显示 “Back to Blog”，且未指向中文导航语义。
  - 首页与 Footer 暂无“教程中心”显性入口，Tools 模块链接单一。
- **优化方向（本阶段讨论）**：
  1. **教程首页结构**：为 `/tutorials` 设计 Hero（强调“Plus 代充 / 镜像 / 避坑指南”）、分类 Tab、推荐/热门列表、FAQ/CTA，并将 `BlogList`、`Headline` 等组件替换为定制布局。
  2. **分类策略**：在 frontmatter 中新增 `category` 值（如 `plus`, `mirror`, `payment`, `guide`），列表页支持按分类/标签筛选或展示专区。
  3. **导航互链**：更新 `ToBlogLink` 文案为“返回教程中心”，并在主导航/页脚/Tools 模块加入指向 `/tutorials` 的入口。
  4. **结构化数据**：待列表页成型后，考虑添加 Breadcrumb schema 与列表页的 FAQ/HowTo 亮点模块。
- **最新进展（导航互链）**：
  - Header 文案更新为 “教程中心”，Footer “教程”栏目链接到三篇核心文章 + “更多教程”。
  - 首页 `ToolsAndTutorials` 模块增加镜像/避坑教程入口，底部按钮改为 “进入教程中心”。
  - `ToBlogLink` 改为“返回教程中心”，确保文章页与列表页互链一致。
- **结构化数据 & 埋点**：
  - 教程列表页与单篇页均输出 Breadcrumb/FAQ JSON-LD，Plus 类文章自动生成 HowTo 数据（来源于 frontmatter）。
  - 新增 frontmatter `faqs`、`howTo` 字段，`src/content/config.ts`、`src/utils/blog.ts`、`src/types.d.ts` 同步支持；已有三篇教程已补齐具体问答与步骤。
  - Hero 及文章 CTA 配置 `data-ga-event` 并通过带缓冲的脚本派发到 GA4 / dataLayer，事件统一归类为 `tutorials_*`，方便追踪内容转化。
  - `Related Posts`、分页按钮等 UI 文案改为中文，保持整体语言一致性。
